# 医院分布系统调用链说明

本文档说明 Qt 端按钮、网页端 JavaScript、百度地图 API、医院 POI 查询、行政区边界绘制、Voronoi 分析和合理性评分之间的调用关系。

## 一、整体结构

系统由两部分组成：

- Qt/C++ 端：负责窗口界面、按钮事件、输入框读取、嵌入网页地图。
- HTML/JavaScript 端：负责百度地图加载、行政区边界查询、医院搜索、Voronoi 绘制、合理性评分。

核心交互方式是：

```text
Qt 按钮事件
-> MapController
-> QWebEnginePage::runJavaScript(...)
-> index.html 中的 JavaScript 方法
-> 百度地图 API / D3 / polygon-clipping
-> 地图覆盖物显示
```

## 二、Qt 到 JavaScript 的调用链

### 1. 获取行政区

```text
MainWindow::MainWindow
-> 点击 regionBtn
-> 读取 searchEdit 文本
-> MapController::loadRegionBoundary(region)
-> QWebEnginePage::runJavaScript("loadRegionBoundary('region')")
-> JavaScript: loadRegionBoundary(region)
```

对应作用：

- `regionBtn` 点击回调：读取用户输入的行政区名称。
- `MapController::loadRegionBoundary`：把行政区名称转义后调用 JS 边界查询入口。
- `loadRegionBoundary`：网页端行政区获取入口，请求边界、缓存边界并显示轮廓。

### 2. 搜索医院

```text
MainWindow::MainWindow
-> 点击 searchBtn
-> 读取 searchEdit 文本
-> MapController::searchHosptial(region)
-> QWebEnginePage::runJavaScript("searchHospitals('region')")
-> JavaScript: searchHospitals(region)
```

对应作用：

- `MainWindow::MainWindow`：初始化主窗口，绑定按钮事件。
- `searchBtn` 点击回调：在已获取行政区边界后，触发医院搜索。
- `MapController::searchHosptial`：把 C++ 字符串转义后拼接为 JS 调用。
- `searchHospitals`：网页端医院搜索主入口，只复用已缓存边界，不重复请求行政区接口。

### 3. 生成 Voronoi 图

```text
MainWindow::MainWindow
-> 点击 voronoiBtn
-> MapController::runVoronoiAnalysis()
-> QWebEnginePage::runJavaScript("runVoronoiAnalysis()")
-> JavaScript: runVoronoiAnalysis()
```

对应作用：

- `runVoronoiAnalysis`：根据当前医院坐标生成 Voronoi 图，并按行政区边界裁剪。

### 4. 合理性评分

```text
MainWindow::MainWindow
-> 点击 calculateBtn
-> MapController::calculateRationalityResult(callback)
-> QWebEnginePage::runJavaScript("return calculateRationality() || window.jsScoreResult")
-> JavaScript: calculateRationality()
-> window.jsScoreResult
-> MainWindow::showScoreResult(score)
-> 右侧评分面板显示总分和详细指标
```

对应作用：

- `calculateRationality`：触发网页端合理性评分。
- `calculateRationalityResult`：在同一次 JS 执行中计算评分并读取 `window.jsScoreResult`。

## 三、行政区边界查询与搜索范围生成

### 1. 行政区边界查询链

```text
loadRegionBoundary(region)
-> getRegionBoundaries(region)
-> BMapGL.Boundary().get(region, callback)
-> parseBaiduBoundary(boundary)
-> currentRegionBoundaries = boundaries
-> drawRegionBoundaries(boundaries)
```

### 2. 边界数据格式

百度返回的行政区边界通常是字符串：

```text
lng,lat;lng,lat;lng,lat;...
```

`parseBaiduBoundary` 会把它转为：

```js
[
  [lng, lat],
  [lng, lat],
  [lng, lat]
]
```

如果行政区有多块边界，最终结构是：

```js
[
  [[lng, lat], [lng, lat], ...],
  [[lng, lat], [lng, lat], ...]
]
```

### 3. 边界转搜索范围

百度 `LocalSearch` 不能直接传入复杂行政区多边形，只能按城市名或矩形 `Bounds` 搜索。因此代码先用行政区边界生成外接矩形，再切成网格。

调用链：

```text
searchAllHospitalPois(region, boundaries)
-> createSearchBoundsList(boundaries)
-> getBoundaryExtent(boundaries)
-> flattenPolygons(boundaries)
-> createBoundsFromExtent(extent)
```

处理逻辑：

```text
行政区边界点
-> 计算 minLng / maxLng / minLat / maxLat
-> 得到完整外接矩形
-> 按 searchGridSize 切成 4 x 4 网格
-> 返回 1 个完整矩形 + 16 个小矩形
```

这样设计的原因：

- 只搜整个城市时，百度结果容易被主城区占满，边缘县区或附属小城市医院可能搜不出来。
- 切成网格后，每个小范围单独搜索，可以提高医院覆盖率。
- 外接矩形只是搜索窗口，最终结果仍会用真实行政区边界过滤。

## 四、医院搜索调用链

```text
searchHospitals(region)
-> 检查 currentRegionBoundaries 是否存在
-> clearVoronoi()
-> clearHospitalResults()
-> drawRegionBoundaries(currentRegionBoundaries)
-> searchAllHospitalPois(region, currentRegionBoundaries)
   -> createSearchBoundsList(boundaries)
   -> searchKeywordPages(region, "医院", boundaries, bounds)
      -> BMapGL.LocalSearch(...)
      -> local.searchInBounds("医院", bounds)
      -> collectPois(results, boundaries)
         -> isPointInRegion(point, boundaries)
            -> isPointInPolygon(point, polygon)
         -> isHospitalPoi(poi)
   -> dedupePois(allPois)
-> renderHospitals(pois)
-> window.lastHospitalSearchInfo = ...
```

搜索特点：

- 医院搜索不再请求行政区边界，必须先点击“获取行政区”。
- 当前关键词只使用 `"医院"`。
- `searchKeywordPages` 会分页获取结果，最多读取 `maxSearchPages` 页。
- `collectPois` 会同时过滤行政区外点和非医院类型。
- `dedupePois` 会去除不同网格重复搜到的同一家医院，并通过名称相似和距离过近过滤同院区重复点。
- `renderHospitals` 会把最终医院点绘制到地图上，并写入 `hospitalData`。

## 五、Voronoi 图分析调用链

```text
runVoronoiAnalysis()
-> clearVoronoiCells()
-> 读取 currentRegionBoundaries
-> computeVoronoiClipped(boundaries)
   -> drawVoronoiCells(boundaries)
      -> mercatorForward(lng, lat)
      -> d3.Delaunay.from(points)
      -> delaunay.voronoi([minX, minY, maxX, maxY])
      -> clipCellWithBoundaries(cellRing, mercBounds)
      -> mercatorInverse(x, y)
      -> BMapGL.Polygon(...)
   -> bringRegionBoundariesToFront(boundaries)
-> bringHospitalOverlaysToFront()
```

Voronoi 原理：

```text
医院坐标作为生成点
-> 两两医院之间形成距离相等的分界线
-> 每个区域内的位置都离对应医院最近
-> 得到医院理论最近服务范围
```

代码中先用医院点生成矩形范围内的 Voronoi，然后用行政区边界裁剪：

```text
Voronoi 单元 ∩ 行政区边界 = 行政区内的医院服务分区
```

这样可以避免生成矩形 Voronoi 图，最终结果只显示在真实行政区轮廓内。

## 六、合理性评分调用链

```text
calculateRationality()
-> analyzeHospitalDistribution(hospitalData, currentRegionBoundaries)
   -> getRegionCenter(boundaries)
   -> polygonAreaMeters(boundaries, origin)
   -> createRegionSamples(boundaries)
   -> nearestHospitalStats(samples, hospitals, origin)
   -> hospitalNearestNeighborDistances(hospitals, origin)
-> coefficientOfVariation(values)
-> scoreByDistance(...)
-> clamp(...)
-> window.jsScoreResult = JSON.stringify(result)
-> Qt 端右侧评分面板显示综合评分和详细指标
```

### 评分算法公式

该评分不是医疗行业官方标准，而是系统内部定义的空间几何评价模型。新版算法更贴合 Voronoi 分析结果，综合评价可达性、Voronoi 服务区均衡、覆盖盲区和医院聚集度。

#### 1. 合理服务半径

先计算行政区面积和医院数量：

```text
areaMeters = 行政区面积，单位平方米
hospitalCount = 医院数量
```

如果医院在行政区内均匀分布，则单家医院理论负责面积为：

```text
areaMeters / hospitalCount
```

由面积反推理论间距：

```text
expectedSpacing = sqrt(areaMeters / hospitalCount)
```

系统中的合理服务半径为：

```text
serviceRadius = clamp(expectedSpacing * 1.35, 3000, 15000)
```

也就是把 `expectedSpacing * 1.35` 限制在 3 km 到 15 km 之间。

`1.35` 是经验放大系数，不是官方标准。`expectedSpacing` 表示医院均匀分布时的理论间距，但真实行政区形状通常不规则，医院也不可能完全均匀分布。因此系统将理论间距适当放大 35%，作为较宽松的服务半径判断标准。

设置 `1.35` 的原因：

```text
1. 如果系数过小，例如 1.0，很多边缘采样点会被过度判定为盲区，评分偏严。
2. 如果系数过大，例如 2.0，服务半径过宽，真实覆盖不足区域不容易暴露。
3. 取 1.35 是折中处理，既允许医院服务范围存在一定重叠，也能保留对覆盖盲区的识别能力。
```

服务半径限制在 3 km 到 15 km 的原因：

```text
1. 面积很小或医院很多时，理论间距可能过小，因此设置 3 km 下限，避免服务半径过窄。
2. 面积很大或医院很少时，理论间距可能过大，因此设置 15 km 上限，避免服务半径过宽导致盲区被掩盖。
```

#### 2. 可达性评分

系统在行政区内生成规则采样点，并计算每个采样点到最近医院的距离：

```text
averageDistance = 所有采样点到最近医院的平均距离
p90Distance = 90% 分位最近距离
maxDistance = 最远采样点到最近医院的距离
```

距离指标使用 `scoreByDistance(value, goodValue, badValue)` 转换成 0 到 100 分：

```text
value <= goodValue 时，得 100 分
value >= badValue 时，得 0 分
中间区间线性递减
```

三个子项分别为：

```text
averageDistanceScore = scoreByDistance(averageDistance, serviceRadius * 0.6, serviceRadius * 1.8)
p90DistanceScore = scoreByDistance(p90Distance, serviceRadius, serviceRadius * 2.5)
maxDistanceScore = scoreByDistance(maxDistance, serviceRadius * 1.5, serviceRadius * 3.5)
```

可达性评分：

```text
accessibilityScore =
  averageDistanceScore * 0.40
+ p90DistanceScore * 0.45
+ maxDistanceScore * 0.15
```

含义：

```text
平均距离反映整体可达性，90% 距离反映大多数区域可达性，最远距离用于惩罚边缘极端盲区。
```

总分权重：

```text
40%
```

#### 3. Voronoi 服务区均衡评分

每个采样点会分配给距离最近的医院，这相当于用采样点近似计算各医院的 Voronoi 服务区面积。

```text
assignmentCounts = [医院1负责采样点数, 医院2负责采样点数, ...]
```

计算各医院服务区大小的变异系数：

```text
voronoiBalanceCv = 标准差 / 均值
```

服务区均衡评分：

```text
voronoiBalanceScore = clamp((1 - min(voronoiBalanceCv, 1.2) / 1.2) * 100, 0, 100)
```

含义：

```text
各医院服务区面积越接近，分数越高；某些医院服务区特别大或特别小，说明分布不均衡，分数越低。
```

总分权重：

```text
25%
```

#### 4. 覆盖盲区评分

如果采样点到最近医院的距离超过合理服务半径，则认为该采样点处于覆盖盲区。

```text
blindSpotCount = 最近医院距离 > serviceRadius 的采样点数量
blindSpotRate = blindSpotCount / 总采样点数量
blindSpotScore = (1 - blindSpotRate) * 100
coverageRate = 1 - blindSpotRate
```

含义：

```text
盲区比例越低，覆盖盲区评分越高。
```

总分权重：

```text
20%
```

#### 5. 医院聚集度评分

系统会计算每家医院到最近另一家医院的距离：

```text
nearestNeighborDistance = 医院到最近其他医院的距离
```

如果该距离小于理论间距的 35%，认为该医院存在过度聚集：

```text
nearestNeighborDistance < expectedSpacing * 0.35
```

`0.35` 是经验聚集阈值，不是官方标准。它表示如果两家医院之间的距离只有理论均匀间距的三分之一左右，就认为它们明显过近，存在空间扎堆现象。

设置 `0.35` 的原因：

```text
1. 如果阈值过小，例如 0.2，只有极近的医院才会被判定为聚集，很多明显集中区域不会被扣分。
2. 如果阈值过大，例如 0.5，医院稍微靠近就会被判定为聚集，扣分过严。
3. 取 0.35 是折中处理，用于识别明显低于均匀分布状态的医院近距离聚集。
```

聚集比例：

```text
clusterRate = 过度聚集医院数量 / 医院数量
```

聚集度评分：

```text
clusteringScore = (1 - clusterRate) * 100
```

含义：

```text
医院越分散合理，聚集度评分越高；医院大量扎堆，聚集度评分越低。
```

总分权重：

```text
15%
```

#### 6. 总分公式

最终综合评分为：

```text
totalScore =
  accessibilityScore * 0.40
+ voronoiBalanceScore * 0.25
+ blindSpotScore * 0.20
+ clusteringScore * 0.15
```

权重分配也是经验权重，不是官方标准。设置依据是空间分布评价中的重要性顺序：

```text
1. 可达性评分 40%：
   医院布局首先要保证行政区内各位置到最近医院的距离不能过远，因此权重最高。

2. Voronoi 服务区均衡评分 25%：
   该指标直接对应 Voronoi 图。如果某些医院服务区过大、某些医院服务区过小，说明资源空间分配不均，因此权重次高。

3. 覆盖盲区评分 20%：
   该指标专门惩罚超过合理服务半径的区域。它与可达性相关，但更关注明显覆盖不足区域，因此单独设为一项。

4. 医院聚集度评分 15%：
   聚集度用于惩罚医院扎堆。医院过度集中会降低合理性，但单独聚集并不能完全代表服务质量，因此作为辅助惩罚项，权重最低。
```

评分结果会写入：

```text
window.jsScoreResult = JSON.stringify(result)
```

Qt 端读取后显示在右侧评分面板中，包括医院数量、行政区面积、服务半径、覆盖率、平均最近距离、90% 距离、最远距离、可达性、服务区均衡、盲区比例、聚集度和综合评分。

该评分只评价空间几何分布，不考虑道路、交通时间、床位数、医院等级和真实人口密度。

## 七、方法作用总表

### Qt/C++ 方法

| 方法 | 作用 |
|---|---|
| `MainWindow::MainWindow` | 初始化界面，创建地图控制器，绑定搜索、Voronoi、评分按钮。 |
| `MainWindow::~MainWindow` | 释放 UI 对象。 |
| `MapController::MapController` | 在 Qt 容器中创建 `QWebEngineView`，加载 `qrc:/index.html`。 |
| `MapController::loadRegionBoundary` | 将行政区名称转义后调用 JS 的 `loadRegionBoundary(region)`。 |
| `MapController::searchHosptial` | 将行政区名称转义后调用 JS 的 `searchHospitals(region)`。 |
| `MapController::runVoronoiAnalysis` | 调用 JS 的 `runVoronoiAnalysis()`。 |
| `MapController::calculateRationality` | 调用 JS 的 `calculateRationality()`。 |
| `MapController::calculateRationalityResult` | 在同一次 JS 调用中完成评分计算和结果读取，避免异步读到空结果。 |
| `MapController::getScoreResult` | 异步读取 JS 变量 `window.jsScoreResult`，返回评分结果。 |
| `MapController::getHospitalSearchState` | 异步读取医院搜索是否完成和医院数量，用于控制 Voronoi、评分按钮是否可用。 |
| `MainWindow::resetScorePanel` | 隐藏并重置右侧合理性评分面板。 |
| `MainWindow::showScoreResult` | 将综合评分、医院数量、面积、覆盖率、距离等指标显示到 WebEngine 右侧的小面板。 |
| `MainWindow::updateScorePanel` | 触发 JS 评分计算并刷新右侧评分面板。 |

### JavaScript 搜索与边界方法

| 方法 | 作用 |
|---|---|
| `loadRegionBoundary` | 行政区获取入口，负责请求行政区边界、缓存边界并显示轮廓。 |
| `searchHospitals` | 医院搜索主入口，负责复用已缓存边界、搜索医院、渲染点位。 |
| `clearHospitalResults` | 清空医院 marker、标签、医院数据和评分卡，但保留行政区边界缓存。 |
| `clearMarkers` | 清空医院 marker、标签、Voronoi 图层、行政区边界和行政区缓存。 |
| `getRegionBoundaries` | 调用百度 `BMapGL.Boundary` 查询行政区边界。 |
| `parseBaiduBoundary` | 把百度边界字符串或点数组统一解析为 `[lng, lat]` 点数组。 |
| `flattenPolygons` | 将多个边界多边形拉平成一个点数组。 |
| `createRegionBounds` | 根据行政区边界生成一个完整百度 `Bounds`。 |
| `getBoundaryExtent` | 计算行政区边界的最小/最大经纬度，即外接矩形。 |
| `createBoundsFromExtent` | 把 `{minLng, maxLng, minLat, maxLat}` 转为 `BMapGL.Bounds`。 |
| `createSearchBoundsList` | 生成完整外接矩形和 4 x 4 网格矩形，用于提高搜索覆盖率。 |
| `searchAllHospitalPois` | 遍历关键词和搜索范围，汇总全部医院 POI 并去重。 |
| `searchKeywordPages` | 对单个关键词和单个范围执行百度分页搜索。 |
| `collectPois` | 从百度搜索结果中收集有效医院，执行边界过滤和类型过滤。 |
| `isPointInRegion` | 判断点是否落在行政区任意边界多边形内。 |
| `isPointInPolygon` | 使用射线法判断点是否位于一个多边形内部。 |
| `isHospitalPoi` | 判断 POI 是否为医院，并排除卫生院、诊所、宠物医院等无关结果。 |
| `dedupePois` | 对多轮搜索结果按名称、地址、坐标去重。 |
| `normalizeHospitalName` | 规范化医院名称，去掉楼区、括号说明和空白。 |
| `poiDistanceMeters` | 近似计算两个医院 POI 之间的米制距离。 |
| `isDuplicateHospitalPoi` | 根据距离、名称包含关系和前缀相似度判断是否为同一家医院。 |
| `hospitalPoiPriority` | 在重复 POI 中优先保留名称更干净的主院区记录。 |
| `renderHospitals` | 在地图上绘制医院 marker 和标签，并写入 `hospitalData`。 |

### JavaScript Voronoi 方法

| 方法 | 作用 |
|---|---|
| `runVoronoiAnalysis` | Voronoi 分析入口，检查医院数量，读取缓存边界，触发裁剪绘制。 |
| `computeVoronoiClipped` | 协调 Voronoi 绘制，只允许按行政区边界裁剪后的结果显示。 |
| `drawVoronoiCells` | 使用 D3 根据医院点生成 Voronoi 单元，并绘制裁剪后的多边形。 |
| `clipCellWithBoundaries` | 使用 `polygon-clipping` 计算 Voronoi 单元和行政区边界的交集。 |
| `drawRegionBoundaries` | 绘制行政区边界线。 |
| `bringRegionBoundariesToFront` | 将行政区边界重新置顶，避免被彩色 Voronoi 区域盖住。 |
| `clearVoronoiCells` | 只清空旧 Voronoi 彩色多边形，不清行政区边界。 |
| `clearRegionBoundaries` | 只清空行政区边界覆盖物。 |
| `clearVoronoi` | 清空旧 Voronoi 多边形和行政区边界覆盖物。 |
| `bringHospitalOverlaysToFront` | 重新叠放医院 marker 和标签，使其显示在彩色区域上方。 |
| `mercatorForward` | 经纬度转 Web Mercator 平面坐标，供 D3 计算。 |
| `mercatorInverse` | Web Mercator 坐标转回经纬度，供百度地图绘制。 |
| `generateColors` | 为 Voronoi 多边形生成不同颜色。 |
| `closeRing` | 确保多边形环首尾闭合，满足裁剪库输入要求。 |

### JavaScript 合理性评分方法

| 方法 | 作用 |
|---|---|
| `calculateRationality` | 合理性评分入口，检查数据并触发评分模型。 |
| `analyzeHospitalDistribution` | 评分核心方法，综合覆盖、距离、均衡、聚集指标计算总分。 |
| `getRegionCenter` | 计算行政区边界中心，作为本地米制投影原点。 |
| `projectLngLatToMeters` | 将经纬度近似转换为米制坐标，用于距离和面积计算。 |
| `polygonAreaMeters` | 计算行政区边界面积。 |
| `createRegionSamples` | 在行政区内部生成规则采样点。 |
| `nearestHospitalStats` | 计算每个采样点到最近医院的距离和医院分配数量。 |
| `hospitalNearestNeighborDistances` | 计算医院之间的最近邻距离，用于判断聚集程度。 |
| `coefficientOfVariation` | 计算变异系数，衡量医院服务分配是否均衡。 |
| `scoreByDistance` | 将距离指标转换为 0 到 100 分。 |
| `quantile` | 计算距离数组的分位数。 |
| `clamp` | 限制数值范围，避免评分超出上下界。 |
| `clearScoreMarker` | 清理旧版本可能残留在地图上的评分卡；当前评分显示在 Qt 右侧面板。 |

## 八、关键数据变量

| 变量 | 作用 |
|---|---|
| `hospitalData` | 保存最终医院数据，是 Voronoi 和评分的核心输入。 |
| `hospitalMarkers` | 保存医院 marker，便于清理和重新叠放。 |
| `voronoiPolygons` | 保存 Voronoi 彩色区域覆盖物。 |
| `boundaryPolygons` | 保存行政区边界覆盖物。 |
| `currentRegion` | 当前搜索的行政区名称。 |
| `currentRegionBoundaries` | 当前行政区边界缓存。 |
| `window.jsScoreResult` | JS 写给 Qt 读取的评分结果。 |
| `window.lastRegionInfo` | 最近一次行政区边界查询的统计信息。 |
| `window.lastHospitalSearchInfo` | 最近一次医院搜索的统计信息。 |
| `window.hospitalSearchFinished` | 标记医院搜索是否结束，供 Qt 轮询按钮状态。 |
| `hospitalKeywords` | 医院搜索关键词，目前只包含 `"医院"`。 |
| `searchGridSize` | 外接矩形切网格数量，目前为 4，即 4 x 4。 |
| `maxSearchPages` | 每个搜索范围最多读取的百度分页数量。 |

## 九、核心结论

医院搜索不是直接用行政区多边形搜索，因为百度 POI 搜索不支持复杂多边形范围。当前实现采用：

```text
点击“获取行政区”
-> 请求并缓存行政区边界
-> 显示行政区轮廓
-> 点击“搜索医院”
-> 外接矩形
-> 4 x 4 网格
-> 百度 Bounds 搜索医院
-> 行政区多边形过滤
-> 医院类型过滤
-> 去重
-> 地图绘制
```

Voronoi 图也不是直接绘制矩形结果，而是：

```text
医院坐标生成 Voronoi
-> 行政区边界裁剪
-> 行政区轮廓内显示
```

因此，医院点决定服务区归属，行政区边界决定最终显示范围。
